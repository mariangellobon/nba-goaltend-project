# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
src/*.egg-info/
.eggs/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
.env

# Jupyter
.ipynb_checkpoints/

# Executed notebooks (large cell outputs — keep repo source notebooks only)
notebooks/**/*_executed.ipynb

# OS / editor
.DS_Store
.mplconfig/

# ── Dataset & generated artifacts (code only on GitHub) ─────────────────────
# Raw CSVs / labels stay on your machine; clone `data/` structure locally.
/data/**

# Fallback: anywhere in repo
*.joblib

# Outputs, TabPFN run folders, dumped pipelines (everything under outputs except stub)
outputs/**
!outputs/.gitkeep

# Torch / HF caches (often large)
.cache/

# LabChart recordings
*.cmbl

# Local LabChart tree name from older docs
2.98 Data/

# Local copies of sibling projects
GoaltendModel/

# Video inputs and rendered outputs (utilities folder)
*.mov
syncing_video_data/*.csv
syncing_video_data/**/*.mov

# Local env overrides
.env.local
